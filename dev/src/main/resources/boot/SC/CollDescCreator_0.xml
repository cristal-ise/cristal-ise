<?xml version="1.0" encoding="UTF-8"?>
<cristalscript>
  <param name='item' type='org.cristalise.kernel.entity.proxy.ItemProxy' />
  <param name='agent' type='org.cristalise.kernel.entity.proxy.AgentProxy' />
  <param name='job' type='org.cristalise.kernel.entity.agent.Job' />
  <output name='errors' type='org.cristalise.kernel.scripting.ErrorInfo' />
  <script language='javascript' name='CollDescCreator'><![CDATA[ var name = job.getOutcome().getField("Name");
var type = job.getOutcome().getField("Type");

var found = false;

try { // check if already exists
	item.getCollection(name);
	found = true;
} catch (e) { }
if (found) throw "Collection "+name+" already exists in this Item Description";

var params = new Array(2);
params[0] = name;
params[1] = type;
agent.execute(item, "AddNewCollectionDescription", params);
 ]]></script>
</cristalscript>